export interface Tournament {
    year: number;
    name: string;
    acronym: string;
    forum_post: string;
    sheet_link: string;
    badge_url: string;
    roles: Array<string>;
    status: string;
}
